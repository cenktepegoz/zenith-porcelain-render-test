services:
  - type: web
    name: zenith-porcelain-test
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: >-
      curl -fsSL
      https://github.com/cenktepegoz/zenith-porcelain-render-test/releases/download/whatsapp-only-2026-08-24/zenith-porcelain-source-whatsapp-only-2026-08-24.tar.gz
      -o /tmp/zenith-source.tar.gz
      && tar -xzf /tmp/zenith-source.tar.gz
      && corepack pnpm install --frozen-lockfile
      && corepack pnpm build
    startCommand: corepack pnpm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: APP_VERSION
        value: whatsapp-only-2026-08-24
